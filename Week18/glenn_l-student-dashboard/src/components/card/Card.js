import React from "react";
// import CardContainer from "./CardContainer";

const Card = (props) => {
    console.log("card ", props);
    return (
        <>
            
        </>
    );
};

export default Card;
